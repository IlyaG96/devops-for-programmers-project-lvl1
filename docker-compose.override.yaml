version: '3.8'

services:
  app:
    build:
      context: ./app
      dockerfile: ./Dockerfile
    volumes:
      - app:/app/
    command: npm run dev
    ports:
      - "8080:8080"

volumes:
  app:
